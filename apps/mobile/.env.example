# API base URL — no trailing slash. Read by src/lib/config.ts; falls back to
# the deployed Fly backend if unset, so this only needs setting for local dev.
# Local backend (act-api running `uvicorn app.main:app --reload` on this machine,
# per act-api/README.md — uvicorn's default port, no --port flag set): http://localhost:8000
# Deployed pilot backend (default if unset): https://act-api-evode.fly.dev
EXPO_PUBLIC_API_BASE_URL=https://act-api-evode.fly.dev

# Supabase (invite-only pilot auth). Leave unset for local/demo dev — the app
# falls back to the existing demo-session flow until both of these are set.
# Kept commented out so `cp .env.example .env` doesn't activate the gate with
# placeholder values (a `<project>` URL crashes createClient at import).
# The publishable key is safe to embed in a mobile client (Supabase's
# anon/publishable key, not a service-role secret).
# EXPO_PUBLIC_SUPABASE_URL=https://<project>.supabase.co
# EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_...

# Fail-closed switch for pilot/production builds. When set (any non-empty
# value), a build without valid Supabase config shows a config-error screen
# instead of silently shipping without the login gate. Set this in the same
# EAS profile change that adds the two Supabase vars above — never separately.
# EXPO_PUBLIC_REQUIRE_AUTH=1
