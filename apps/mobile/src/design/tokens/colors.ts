// Design-system colors — canonical source is src/theme/colors.ts (the whole app
// already imports from there). Re-exported here so new design/ primitives can
// pull the full token set from one place without a mass import rewrite.
export { colors, type Colors } from '../../theme/colors';
